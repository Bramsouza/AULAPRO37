//classe que armazena e recupera informações do jogador, 
//tanto vindas do banco de dados quanto idas para o banco de dados

class Player {
  constructor() {
    //salva o nome do jogador
   
    //dá um id único para cada jogador
    
    //armazenar a posição x e y de cada jogador.
    
  }

  //getCount() - método para atualizar o campo ‘playerCount’ no banco de dados
  getCount() {
    //Para ler ou escrever no banco de dados
    //.ref() - fornece a localização do campo no banco de dados
    
    //.on - ouve as mudanças que acontecem no campo ‘playerCount’ do banco de dados.
    
      //.val() - copiar o valor do banco de dados para a variável global do código.
      
    
  }

  //updateCount() - método para atualizar o campo ‘playerCount’ no banco de dados
  updateCount(count) {
    // “/” é usado em updateCount para se referir ao diretório raiz
    
  }

  //Temos dois jogadores em jogo
  //1º jogador no lado esquerdo da tela
  //2º no lado direito da tela antes do início da corrida
  addPlayer() {
    //Cria a hierarquia dos jogadores/jogador1 no bd
    
    //Para dar posiçãox para ambos os jogadores, um do centro-esquerda(width/2) e o outro na centro-direita
    

    //Atualizando o campo no bd
    //.ref() fornece a localização no banco de dados.
    //databaseReference.set() cria e salva a referência do banco de dados.
    
  }

  //função para obter todas as informações dos jogadores.
  //As funções estáticas não ficam anexadas a cada objeto da classe
  

}
